import "native/os"
import "native/path"
import "io"

const inputPath str = path::resolve(path::dirname(__FILE__), "input.txt")

def run(input str) {
  io::printLn(input)
}

run("example-input")
# run(os::readFile(inputPath))
